{
  "name": "docker-dev-dash",
  "version": "1.3.3",
  "description": "View log output from all docker containers",
  "homepage": "https://github.com/dbroadhurst/docker-dev-dash",
  "scripts": {
    "start": "node index.js",
    "watch": "pm2 start index.js --watch --ignore-watch node_modules --no-daemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "docker",
    "logs",
    "dashboard"
  ],
  "author": "David Broadhurst",
  "license": "MIT",
  "bin": {
    "docker-dev-dash": "index.js"
  },
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.15.3"
  }
}
